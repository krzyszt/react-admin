"use strict";

var React = require('react');

var Home = React.createClass({
    render: function(){
        return (
            <div className="jumbotron">
                <h1>React Admin App</h1>
                <p>React, React Router and Flux for Admin App</p>
            </div>


        );
    }
});

module.exports = Home;
